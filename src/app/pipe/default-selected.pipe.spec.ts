import { DefaultSelectedPipe } from './default-selected.pipe';

describe('DefaultSelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new DefaultSelectedPipe();
    expect(pipe).toBeTruthy();
  });
});

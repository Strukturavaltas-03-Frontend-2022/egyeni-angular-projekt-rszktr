import { FilterPlatformPipe } from './filter-platform.pipe';

describe('FilterPlatformPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPlatformPipe();
    expect(pipe).toBeTruthy();
  });
});

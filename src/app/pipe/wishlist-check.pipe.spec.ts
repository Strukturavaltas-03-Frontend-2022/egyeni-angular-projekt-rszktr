import { WishlistCheckPipe } from './wishlist-check.pipe';

describe('WishlistCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new WishlistCheckPipe();
    expect(pipe).toBeTruthy();
  });
});

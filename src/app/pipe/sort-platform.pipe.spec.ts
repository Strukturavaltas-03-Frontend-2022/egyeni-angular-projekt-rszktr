import { SortPlatformPipe } from './sort-platform.pipe';

describe('SortPlatformPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPlatformPipe();
    expect(pipe).toBeTruthy();
  });
});

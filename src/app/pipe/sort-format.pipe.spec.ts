import { SortFormatPipe } from './sort-format.pipe';

describe('SortFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SortFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
